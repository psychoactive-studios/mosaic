import React from "react";

const MainCard = ({ onFlip }) => {
  return (
    <div>
      Main card (front)
      <br />
      {/* <button onClick={onFlip}>Flip</button> */}
    </div>
  );
};

export default MainCard;
